---
title: "Taller 1 en grupo"
author: 
- "Alejandro Bedoya Taborda"
- "Angie Melissa Montoya Urrego" 
- "Cristian Camilo Henao Rojas" 
- "Mariana Arias Montoya" 
- "Luis Felipe Marin Buitrago"
date: "28/9/2020"
output: 
  html_document:
    theme: yeti
        
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE,
                      message = FALSE,
                      warning = FALSE)
```


```{r}
#librerias
library(knitr)
library(ggplot2)
library(RColorBrewer)
library(tidyverse)
library(fmsb) #Librería para la araña
library(sqldf) #Librería sql

#lectura de la base de datos.
encuesta_habitos <- read.csv2("Encuesta_habitos.csv", 
                       encoding = "UTF-8",
                       dec = ".")
```
# Cambios de la base de datos

Los cambios realizados en la base de datos se hicieron para un mejor análisis y una mejor manipulación de esta, se le añadieron dos nuevas columnas correspondientes a la depuración de la variable Carrera y la depuración de la variable Fecha de Nacimiento. De forma indirecta, en la variable de Hábitos de estudio se usaron expresiones regulares guardadas en variables de R



# Genero {.tabset}


```{r}
#tabla de frecuencias de género.
genero <- table(encuesta_habitos$genero)
genero_porcentaje <- round(genero / sum(genero)*100,2)
```

## Grafica
```{r}
#Trabajamos el lienzo
par(mar = c(8, 5, 3, 3))# le damos margenes
plot(seq(0,3,length.out = 100), 1:100, 
     type = "n",
     main = "Genero",
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,100,10), col = "gray", lty = 3)
g1 <- barplot(genero_porcentaje,
        col = c("#00afb9", "#fed9b7"),
        cex.names = 0.65,
        las=1,
        add = TRUE,
)
text(g1, c(30,78), 
     labels = paste(formatC(genero_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))
```

## Tabla de frecuencia
```{r}
kable(genero, col.names = c('Género','Frecuencia'))
```


## Análisis descriptivo:

Del gráfico se observa la distribución porcentual de los estudiantes por genero. Se observa que la cantidad de personas que pertenecen al genero masculino es evidentemente mayor a la cantidad de personas del genero fememino, esto es con una diferencia del 46.96% entre ambos generos. 


# Fecha de nacimiento {.tabset}

## Gráfica
```{r}


fechas <- as.Date(encuesta_habitos$fecha_dep, "%d/%m/%Y")
fec_table <- table(format(fechas, "%Y"))
e <- kable(fec_table,
           col.names = c('Año', 'Frecuencia'),
           align = 'lc')

ggplot(encuesta_habitos, aes(x = seq(0, 100, length.out = 132), y = as.numeric(format(fechas, "%Y")))) +
  geom_boxplot(fill = "slateblue", alpha = 0.4) +
  theme_bw() +
  xlab("") + ylab('Año') + ggtitle('Boxplot de la variable Años') +
  theme(plot.title = element_text(
    family = "Helvetica",
    face = "bold",
    size = (20),
    hjust = .5
  ))  +
  coord_cartesian(ylim = c(1956, 2020)) + scale_y_continuous(breaks = seq(1950, 2020, 5)) +
  theme(
    axis.text.x = element_blank()
    ,
    axis.ticks.x = element_blank()
    ,
    axis.title.y = element_text(size = 15, vjust = 1.3),
    axis.text.y = element_text(
      family = "Helvetica",
      face = "bold",
      size = (10)
    )
  )

ggplot(encuesta_habitos, aes(x = seq(0, 100, length.out = 132), y = as.numeric(format(fechas, "%Y")))) +
  geom_violin(fill = "slateblue", alpha = 0.4) +
  theme_bw() +
  xlab("") + ylab('Año') + ggtitle('Violín de la variable Años') +
  theme(plot.title = element_text(
    family = "Helvetica",
    face = "bold",
    size = (20),
    hjust = .5
  )) +
  coord_cartesian(ylim = c(1956, 2020)) + scale_y_continuous(breaks = seq(1950, 2020, 5)) +
  theme(
    axis.text.x = element_blank(),
    axis.ticks.x = element_blank(),
    axis.title.y = element_text(size = 15, vjust =       1.3),
    axis.text.y = element_text(
      family = "Helvetica",
      face = "bold",
      size = (10)
    )
  )
```


## Resumen numerico

```{r}
summary(as.numeric(format(fechas,"%Y")))
```

## Tabla de frecuencia
```{r}
e
```

## Grafica Mes-Dias
```{r}
mes <- as.vector(table(format(fechas, '%m')))
dff <- data.frame('mes' = mes)
dtable2 <- sort(unique(format(fechas, '%m')))


ggplot(dff, aes(x = dtable2, y = mes)) +
  geom_segment(aes(
    x = dtable2,
    xend = dtable2,
    y = 0,
    yend = mes
  ),
  color = 'grey70',
  lwd = 1) +
  theme_light() +
  scale_x_discrete(
    labels = c(
      'Enero',
      'Febrero',
      'Marzo',
      'Abril',
      'Mayo',
      'Junio',
      'Julio',
      'Agosto',
      'Septiembre',
      'Octubre',
      'Noviembre',
      'Diciembre'
    )
  ) +
  scale_y_continuous(breaks = seq(0, 16, 1)) +
  ggtitle('Lollipop de la variable Mes') +
  theme(
    plot.title = element_text(
      family = "Helvetica",
      face = "bold",
      size = (20),
      hjust = .5
    ),
    axis.title.x = element_text(size = (15), vjust = 7),
    axis.text.y = element_text(
      family = "Helvetica",
      face = "bold",
      size = (10)
    ),
    axis.text.x = element_text(
      family = "Helvetica",
      face = "bold",
      size = (10),
      angle = 45
    ),
    axis.title.y = element_text(size = 15),
    panel.grid.major.x = element_blank(),
    panel.border = element_blank(),
    axis.ticks.x = element_blank()
  ) +
  ylab('Número de personas') + xlab('Mes') +
  geom_point(color = "brown", size = 3)


dff <- data.frame('mes' = as.vector(format(fechas, '%m')),
                  'dia' = as.vector(format(fechas, '%d')))
dias_sep <- subset(dff, mes == '09')

ggplot(as.data.frame(sort(unique(dias_sep$dia))), aes(x = sort(unique(dias_sep$dia)), y =
                                                        as.vector(table(dias_sep$dia)))) +
  geom_segment(aes(
    x = sort(unique(dias_sep$dia)),
    xend = sort(unique(dias_sep$dia)),
    y = 0,
    yend = as.vector(table(dias_sep$dia))
  ),
  color = 'grey70',
  lwd = 1) +
  theme_light() +
  scale_y_continuous(breaks = seq(0, 16, 1)) + ggtitle('Lollipop del mes de Septiembre') +
  theme(
    plot.title = element_text(
      family = "Helvetica",
      face = "bold",
      size = (20),
      hjust = .5
    ),
    axis.title.x = element_text(size = (15), vjust = -1),
    axis.text.y = element_text(
      family = "Helvetica",
      face = "bold",
      size = (10)
    ),
    axis.text.x = element_text(
      family = "Helvetica",
      face = "bold",
      size = (10)
    ),
    axis.title.y = element_text(size = 15),
    panel.grid.major.x = element_blank(),
    panel.border = element_blank(),
    axis.ticks.x = element_blank()
  ) +
  ylab('Número de personas') + xlab('Día de Septiembre') +
  geom_point(color = "orange", size = 3)
```

## Análisis Descriptivo

Del gráfico se observa la distribución de los estudiantes por año de nacimiento en un boxplot. De ahí se pueden sacar datos como los son sus datos atípicos, rango intercuartil, mediana, su mínimo y máximo. Se puede ver notoriamente dos datos mucho más atípicos que los otros,  los cuáles son, 1) el del 2020, que es evidente, porque nos estaban preguntando por nuestra fecha de nacimiento y una persona que apenas nació no podría estudiar y 2) el del 1955, porque es un dato extraño o muy exótico, comparado con los otros datos que se muestra. También nos muestra la mediana, que corresponde al año 2000 y su rango intercuartil es 3, ya que al restar el primer cuartil (1998), con el tercer cuartil (2001) obtenemos este número. Su año mínimo es 1956 y su año máximo 2020.

GRÁFICA MES-DÍAS: según nuestra gráfica distribuida por los meses del año, y cuánta frecuencia hay en cada uno, se observa considerablemente en el mes de septiembre hay 16 estudiantes, siendo esta la mayor frecuencia  y en los meses de febrero y abril con 4 estudiantes, siendo estos la menor frecuencia. 

MES DE SEPTIEMBRE: tomamos este mes que fue el que tuvo  una mayor frecuencia, y los representamos con sus días, el día, que tuvo una mayor frecuencia fue el 07, con 3 estudiantes. 

# Edad {.tabset}

## Grafica

```{r}
fechadepu<- fechas
fechadepu<-fechadepu[!format(fechadepu,"%Y") %in% as.character(seq(2005,2020,1))]
edad<-round(as.numeric(Sys.Date() - fechadepu )/ 365,0)

ggplot(as.data.frame(edad), aes(x=seq(0,100,length.out = 125),y=edad)) + 
    geom_boxplot(fill="darkorange", alpha=0.4) + 
    theme_grey() +
    xlab("") + ylab('Años') + ggtitle('Boxplot de la Edad') + 
    theme(plot.title = element_text(family = "Helvetica", face = "bold", size = (20),hjust = .5)) + 
    coord_cartesian(ylim = c(15,70)) + scale_y_continuous(breaks = seq(15,70,5)) + 
    theme(axis.text.x = element_blank(), 
    axis.ticks.x = element_blank(), 
    axis.title.y = element_text(size = 15, vjust = 1.3), 
    axis.text.y = element_text(family = "Helvetica", face = "bold", size = (10)))
    
ggplot(as.data.frame(edad), aes(x=seq(0,100,length.out = 125),y=edad)) + 
    geom_violin(fill="darkorange", alpha=0.4) + theme_grey() +
    xlab("") + ylab('Años') + ggtitle('Violín de la Edad') + 
    theme(plot.title = element_text(family = "Helvetica", face = "bold", size = (20),hjust = .5))+ 
    coord_cartesian(ylim = c(15,70)) + scale_y_continuous(breaks = seq(15,70,5)) +
    theme(axis.text.x = element_blank(), 
    axis.ticks.x = element_blank(), 
    axis.title.y = element_text(size = 15, vjust = 1.3),
    axis.text.y = element_text(family = "Helvetica", face = "bold", size = (10)))
```



## Resumen numérico
```{r}
summary(edad)
 
```

## Análisis descriptivo

De este gráfico se puede ver la distribución de los estudiantes por edad, en un boxplot. De este boxplot se puede sacar datos  como los son sus atípicos, mediana, rango intercuartil, su mínimo y máximo. Se puede observar que su dato más atípico es 64 años. También que su mediana es 20 años y su rango intercuartil es 4, ya que al restar el primer cuartil (19), con el tercer cuartil (23) obtenemos este número. La edad mínima es 17 años y la edad máxima 64 años.


# Carrera {.tabset}


```{r}
carrera <- table(encuesta_habitos$carrera_dep)
carrera_porcentaje <- round(carrera/sum(carrera) * 100,2)
```

## Grafica
```{r}
#Trabajamos el lienzo
par(mar = c(10, 8, 3, 3))# le damos margenes
plot(seq(0,10,length.out = 100), 1:100, 
     type = "n",
     main = "Carrera",
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,100,10), col = "gray", lty = 3)
g2 <- barplot(carrera_porcentaje,
        col = c("#f94144", "#f3722c","#f8961e", "#f9c74f","#90be6d", "#43aa8b","#577590", "#00b4d8"),
        las=1,
        names.arg = "",
        add = TRUE,
)
text(g2, c(60,10,8,8,45,10,10,8), 
     labels = paste(formatC(carrera_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))
text(g2 + 0.2,
    par("usr") [3], 0.4,
    labels = names(carrera),
    srt = 45,
    pos = 2,
    cex = 0.60,
    xpd = TRUE)
```

## Tabla de frecuencia
```{r}
kable(sort(carrera,decreasing = TRUE),col.names = c('Carrera','Frecuencia'),align = 'lc')
```


## Análisis descriptivo:

En este gráfico se observa la distribución porcental de los estudiantes por carrera. Se puede notar que apróximadamente el 90% de los estudiantes pertenecen a las carreras de Estadística e ingeniería de sistemas e informática y con menos frecuencia se encuentran las carreras de ingeniería biológica e ingeniería de control.

# PAPA {.tabset}

## Grafica
```{r}
p<- sort(encuesta_habitos$PAPA)
frecpapa<-as.vector(table(p))
p<-unique(p)
dfpapa<- data.frame(papa=p,frecuencia=frecpapa)

ggplot(encuesta_habitos, aes(encuesta_habitos$PAPA)) +
geom_freqpoly(bins=20,color='#e71d36',lwd=1) + 
ggtitle('Polígono de Frecuencia del PAPA') +
scale_y_continuous(breaks = seq(0,20,2)) + ylab('Frecuencia') + xlab('P.A.P.A')+ 
theme(plot.title = element_text(family = "Helvetica", face = "bold", size = (20),hjust = .5))+ 
theme(axis.ticks.x = element_blank(),
axis.text.x = element_text(family = "Helvetica", face = "bold", size = (10)), 
axis.text.y = element_text(family = "Helvetica", face = "bold", size = (10)), 
axis.title.x = element_text(size = 15),axis.title.y = element_text(size = 15))
```


## Tabla de Frecuencia
```{r}
kable(table(round(encuesta_habitos$PAPA,1)), col.names = c('PAPA','Frecuencia'), align = 'cc')
```

## Análisis descriptivo:

De la gráfica se puede observar claramente una mayor concentración de frecuencia en los valores del PAPA correspondientes al intervalo de 3.5 a 4, siendo el valor del PAPA igual a 3.8 el de mayor frecuencia.

# Estrato {.tabset}

```{r}
estrato <- table(encuesta_habitos$estrato)
estrato_porcentaje <- round(estrato/sum(estrato)*100,2)
```

## Gráfica
```{r}
#Trabajamos el lienzo
par(mar = c(8, 5, 3, 3))# le damos margenes
plot(seq(0,7,length.out = 100), 1:100, 
     type = "n",
     main = "Estrato",
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,100,10), col = "gray", lty = 3)
g1 <- barplot(estrato_porcentaje,
        col = c("#f94144", "#f3722c","#f8961e", "#f9c74f","#90be6d", "#43aa8b","#577590"),
        cex.names = 0.65,
        las=1,
        add = TRUE,
)
text(g1, c(8,20,40,50,12,8), 
     labels = paste(formatC(estrato_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))
```

## Tabla de frecuencia
```{r}
kable(estrato,col.names = c('Estrato','Frecuencia'),align = 'cc')
```


## Análisis descriptivo:

Del gráfico se observa la distribución porcentual de los estudiantes por estrato. Se puede evidencia que aproximadamente el 78% de los estudiantes pertenecen a los estratos 2 y 3.



# Computador de mesa {.tabset}

```{r}
computador_mesa <- table(encuesta_habitos$computador_mesa)
computador_mesa_porcentaje <- round(computador_mesa/sum(computador_mesa)*100,2)
```

## Gráfica
```{r}
#Trabajamos el lienzo
par(mar = c(8, 5, 3, 3))# le damos margenes
plot(seq(0,5,length.out = 100), 1:100, 
     type = "n",
     main = "Computador de mesa",
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,100,10), col = "gray", lty = 3)
g1 <- barplot(computador_mesa_porcentaje,
        col = c("#f94144", "#f3722c","#f8961e", "#f9c74f","#90be6d", "#43aa8b","#577590"),
        cex.names = 0.65,
        las=1,
        add = TRUE,
)
text(g1, c(75,8,8,35), 
     labels = paste(formatC(computador_mesa_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))
```

## Tabla de frecuencia
```{r}
kable(computador_mesa,col.names = c('Respuesta','Frecuencia'),align = 'lc')
```


## Análisis descriptivo:

De este gráfico claramente se observa la distribución porcentual de los estudiantes respecto a si poseen o no computador de mesa. En caso de tenerlo, si es prestado, alquilado o propio. 
Según lo que vemos en la gráfica podemos concluir que casi el 70% de los estudiantes no tienen un computador de mesa.

# Portatil {.tabset}

```{r}
portatil <- table(encuesta_habitos$portatil)
portatil_porcentaje <- round(portatil/sum(portatil)*100,2)
```

## Gráfica
```{r}
#Trabajamos el lienzo
par(mar = c(8, 5, 3, 3))# le damos margenes
plot(seq(0,5,length.out = 100), 1:100, 
     type = "n",
     main = "Portatil",
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,100,10), col = "gray", lty = 3)
g1 <- barplot(portatil_porcentaje,
        col = c("#f94144", "#f3722c","#f8961e", "#f9c74f","#90be6d", "#43aa8b","#577590"),
        cex.names = 0.65,
        las=1,
        add = TRUE,
)
text(g1, c(18,8,16,78), 
     labels = paste(formatC(portatil_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))
```

## Tabla de frecuencia
```{r}
kable(portatil, col.names = c('Respuesta','Frecuencia'),align = 'lc')
```


## Análisis descriptivo:

En este gráfico se observa la distribución porcental de los estudiantes respecto a si poseen o no computador portatil. En caso de tenerlo, si es prestado, alquilado o propio.
Claramente se observa que aproximadamente el 73% de los estudiantes tienen un computador portatil propio.

# Internet {.tabset}

## Gráfica
```{r}
internet <- table(encuesta_habitos$internet)
internet_porcentaje <- round(internet/sum(internet)*100,2)
```

```{r}
#Trabajamos el lienzo
par(mar = c(8, 5, 3, 3))# le damos margenes
plot(seq(0,7,length.out = 100), 1:100,
     type = "n",
     main = "Internet",
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,100,10), col = "gray", lty = 3) # las lineas que señalan las gráficas.
g1 <- barplot(internet_porcentaje,
        col = c("#f94144", "#f3722c","#f8961e", "#f9c74f","#90be6d", "#43aa8b","#577590"), #Colores de la gráfica
        names.arg = "",
        las=1, #Posición de los nombres
        add = TRUE, # se agrega para poder poner encima de un plot.
)
# Con las siguientes líneas se trabajan los porcentajes encima de la barra.
text(g1, c(10,8,10,10,38,60), #Aquí pueden modificar las alturas.
     labels = paste(formatC(internet_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))

text(g1 + 0.2,
    par("usr") [3], 0.4,
    labels = names(internet),
    srt = 45,
    pos = 2,
    cex = 0.65,
    xpd = TRUE)

```

## Tabla de frecuencia:
```{r}
kable(internet,col.names = c('Respuesta','Frecuencia'),align = 'lc')
```


## Análisis descriptivo:

En la gráfica se observa la distribución porcentual de los estudiantes respecto a qué tipo de internet poseen, si es alquilado, de “contrabando”, prestado, propio, familiar o si es un plan de datos en el celular. Se puede ver de forma evidente que la mayoría de los estudiantes, con un aproximado del 55% poseen internet de clasificación familiar. 


# Smartphone {.tabset}


```{r}
smartphone <- table(encuesta_habitos$smartphone)
smartphone_porcentaje <- round(smartphone/sum(smartphone)*100,2)
```

## Gráfica

```{r}
#Trabajamos el lienzo
par(mar = c(5, 5, 1.5, 3))# le damos margenes
plot(seq(0,5,length.out = 100), 1:100,
     type = "n",
     main = "Smartphone", 
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,100,10), col = "gray", lty = 3) # las lineas que señalan las gráficas.
g1 <- barplot(smartphone_porcentaje,
        col = c("#f94144", "#f3722c","#f8961e", "#f9c74f","#90be6d", "#43aa8b","#577590"), #Colores de la gráfica
        cex.names = 0.65,#tamaño del nombre
        las=1, #Posición de los nombres
        add = TRUE, # se agrega para poder poner encima de un plot.
)
# Con las siguientes líneas se trabajan los porcentajes encima de la barra.
text(g1, c(6,6,10,102), #Aquí pueden modificar las alturas.
     labels = paste(formatC(smartphone_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))
```

## Tabla de frecuencia
```{r}
kable(smartphone,col.names = c('Respuesta','Frecuencia'),align = 'lc')
```

## Análisis descriptivo:

Del gráfico se muestra la distribución porcentual de los estudiantes respecto a si poseen o no un smartphone. En caso de tenerlo, si es prestado, alquilado o propio. Se observa claramente que un aproximado del 97% de los estudiantes tienen un smartphone propio. 

# Origen  del Covid-19 {.tabset}

## Distribución porcentual

```{r}
a<-encuesta_habitos$origen_covid_19
a<-gsub('\\<Es.*n[[:punct:]]','Conspiracion',a)
a<-gsub('\\<Fue[[:blank:]]fabricado.*rio[[:punct:]]','Laboratorio',a)
a<-gsub('\\<Fue.*nos[[:punct:]]','Animales',a)
a<-gsub('\\<No.*do[[:punct:]]','Gobiernos',a)
a<-gsub('\n',', ',a)
aporc<-round(table(a)/sum(table(a))*100,1)
q<-as.data.frame(sort(table(a),decreasing = TRUE))
palets<- brewer.pal(8,"Set2")

pie(sort(aporc,decreasing = TRUE), 
main='Causas del Covid-19', col=palets, border = 'white', lty=1,radius = 1,labels = '')
legend(1,0,legend = paste(as.character(sort(aporc,decreasing = TRUE)),'%'), pch=15, col=palets, cex = .7)
legend(-2,1,legend = q$a , pch=15, col=palets, cex = .7)



```


### Tabla de frecuencia

```{r}
kable(round(sort(table(a),decreasing = TRUE)/sum(table(a))*100,1),col.names = c('Causa','porcentaje'))

```



## Grafica

```{r}
q<-as.data.frame(table(a))

ggplot(q,aes(x=q$Freq,y=q$a)) + 
geom_bar(stat = "identity", col='darkslategray3',fill='darkslategray3',alpha=.7) +
scale_x_continuous(breaks = seq(0,80,5)) + ylab('Causa') + xlab('Número de personas') +
ggtitle('Causas del Covid-19') +
theme(panel.grid.minor.y = element_blank(),
panel.grid.minor.x = element_blank(), 
axis.ticks.y = element_blank(), 
axis.title.x = element_text(size=15, vjust = -.5), 
axis.title.y = element_text(size = 15,vjust = 3), 
axis.text = element_text(family = "Helvetica", face = "bold", size = (10)), 
plot.title = element_text(family = "Helvetica", face = "bold", size = (20),hjust = .5)
)
```


## Análisis descriptivo:
De las gráficas anteriores se pueden observar las diferentes opciones sobre la posible causa del covid-19. La opción con mayor porcentaje fue “Animales” con un aproximado de 56%, lo que quiere decir que alrededor de 65 estudiantes creen que el origen del covid-19 fue un resultado de pasar el virus de animales a los humanos. Así mismo, con un porcentaje muy significativo de maso menos el 28% se encuentra la opción de “laboratorio”, es decir que 33 personas creen que el covid-19 fue creado en un laboratorio y, alrededor el 11.2% considera que el origen del covid-19 se debió a varias opciones. 



# Estadisticas del gobierno {.tabset}

```{r}
gobierno <- table(encuesta_habitos$estad_gov)
```

```{r}
gobierno_porcentaje <- round(gobierno/sum(gobierno)*100,2)
```

## Gráfica
```{r}
#Trabajamos el lienzo
par(mar = c(5, 5, 3, 3))# le damos margenes
plot(seq(0,5,length.out = 100), 1:100,
     type = "n",
     main = "Estadistica del gobierno.", 
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,100,10), col = "gray", lty = 3) # las lineas que señalan las gráficas.
g1 <- barplot(gobierno_porcentaje,
        col = c("#f94144", "#f3722c"), #Colores de la gráfica
        cex.names = 0.65,#tamaño del nombre
        las=1, #Posición de los nombres
        add = TRUE, # se agrega para poder poner encima de un plot.
)
# Con las siguientes líneas se trabajan los porcentajes encima de la barra.
text(g1, c(78,30), #Aquí pueden modificar las alturas.
     labels = paste(formatC(gobierno_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))
```

## Tabla de frecuencia
```{r}
kable(gobierno,col.names = c('Respuesta','Frecuencia'),align = 'cc')
```

## Tabla porcentual
```{r}
kable(gobierno_porcentaje,col.names = c('Respuesta','Porcentaje'),align = 'cc')
```


## Análisis descriptivo:

Como se puede evidenciar en la gráfica, aproximadamente el 75% de los estudiantes se muestran escépticos frente a los datos y las estadísticas que emite el gobierno nacional durante el desarrollo de la pandemia en Colombia.

# Hábitos de estudio{.tabset}

## Grafica
```{r}
hab<-c(encuesta_habitos$habito_1,encuesta_habitos$habito_2,encuesta_habitos$habito_3,encuesta_habitos$habito_4,encuesta_habitos$habito_5,encuesta_habitos$habito_6,encuesta_habitos$habito_7,encuesta_habitos$habito_8,encuesta_habitos$habito_9,encuesta_habitos$habito_10,encuesta_habitos$habito_11,encuesta_habitos$habito_12,encuesta_habitos$habito_13,encuesta_habitos$habito_14,encuesta_habitos$habito_15,encuesta_habitos$habito_16,encuesta_habitos$habito_17)

b<- gsub('^Algunas.*gos.$',' Estudia con amigos y solo', hab)
b<-gsub('^Escucha\\D*estudia.$','Estudia con música', b)
b<-gsub('^Estudia.*compañeros.$','Estudia solo soluciones',b)
b<-gsub('^Estudia.*anterior.*examen.$','Estudia solo el día anterior',b)
b<-gsub('^Estudia.*misma.*estudio.$','Ve temas distintos en una sesión',b)
b<-gsub('^Hace.*horario.$','Horario estricto',b)
b<-gsub('^Lee.*texto.$','Lee varias veces un capitulo',b)
b<-gsub('^Los.*estudio.$','Ejercicios con amigos',b)
b<-gsub('^Prefiere.*ejercicios.$','Vistazo a muchos ejercicios resueltos',b)
b<-gsub('^Prefiere.*resueltos.$','Pocos ejercicios bien entendidos',b)
b<-gsub('^Resuelve.*anticipadamente.$','Hace ejercicios sin ver las respuestas',b)
b<-gsub('.*lugar.*fijo.*','Estudia en lugar fijo',b)
b<-gsub('^Solo.*resolver.$','Pregunta a amigos por ejercicios que no pudo',b)
b<-gsub('^Toma.*continuo.$','Hace pausas activas',b)
b<-gsub('^Tomas.*clase.$','Transcribe ejercicios de clase',b)
b<-gsub('^Usa.*resueltos.$','Ve ejercicios hechos en solucionarios',b)
h<- as.data.frame(table(b))
ggplot(h,aes(x=h$Freq,y=h$b)) + 
geom_bar(stat = "identity", col='palevioletred1',fill='palevioletred1',alpha=.7) +
ylab('Hábito') + xlab('Número de personas') +
scale_x_continuous(breaks = seq(0,100,10))+
ggtitle('Hábitos de estudio') +
theme(panel.grid.minor.y = element_blank(),
panel.grid.minor.x = element_blank(), 
axis.ticks.y = element_blank(), 
axis.title.x = element_text(size=15, vjust = -.5), 
axis.title.y = element_text(size = 15,vjust = 3), 
axis.text = element_text(family = "Helvetica", face = "bold", size = (10)), 
plot.title = element_text(family = "Helvetica", face = "bold", size = (20),hjust = .5)
)
```


## Tabla de frecuencia
```{r}
kable(table(b),col.names = c('Hábito','Frecuencia'),align = 'lc')

```

## Análisis descriptivo:

Del gráfico se observa la frecuencia de los estudiantes por hábitos de estudio. Se observa que los hábitos de estudio más usados por los estudiantes son los correspondientes a hacer pausas activas, hacer ejercicios sin ver las respuestas y transcribir ejercicios de clase. Por el lado contrario, el hábito de estudio con menos frecuencia es estudiar solo las soluciones, está cuenta con una frecuencia de 3. 


# Número aleatorio {.tabset}

```{r}
aleatorio <- table(encuesta_habitos$aleatorio)
```

```{r}
aleatorio_porcentaje <- round(aleatorio/sum(aleatorio)*100,2)
```

## Gráfica
```{r}
#Trabajamos el lienzo
par(mar = c(5, 5, 1, 0))# le damos margenes
plot(seq(0,13,length.out = 40), 1:40,
     type = "n",
     main = "Número aleatorio", 
     cex.main = 0.75,
     axes = FALSE,
     xlab = "",
     ylab = "Porcentajes"
     )
abline(h = seq(0,40,10), col = "gray", lty = 3) # las lineas que señalan las gráficas.
g1 <- barplot(aleatorio_porcentaje,
        col = c("#f94144", "#f3722c","#f8961e", "#f9c74f","#90be6d", "#43aa8b","#577590"), #Colores de la gráfica
        cex.names = 0.60,#tamaño del nombre
        las=1, #Posición de los nombres
        add = TRUE, # se agrega para poder poner encima de un plot.
)
# Con las siguientes líneas se trabajan los porcentajes encima de la barra.
text(g1, c(8,10,10,12,14,9,28,17,12,10), #Aquí pueden modificar las alturas.
     labels = paste(formatC(aleatorio_porcentaje,
                            digits = 6, decimal.mark = ",", 
                            big.mark = "."), "%"))
```

## Tabla de frecuencia 
```{r}
kable(aleatorio,col.names = c('Numero','Frecuencia'),align = 'cc')
```

## Tabla porcentual
```{r}
kable(aleatorio_porcentaje,col.names = c('Numero','Porcentaje'),align = 'cc')
```

## Análisis descriptivo:

Como se puede evidenciar en la gráfica, aproximadamente el 40% de los estudiantes, escogieron el número 7 u 8, como su número aleatorio, con una mayor frecuencia en el número 7


# PAPA-Género {.tabset}

## Gráfica
```{r}
#Tabla de frecuencias del papa por genero
papaxgenero <- table(encuesta_habitos$genero, round(encuesta_habitos$PAPA,1))
#Data frame para poder ingresar a la gráfica
total <- data.frame(papaxgenero)
#Gráfica
ggplot(total, aes(fill=Var1, x=Var2, y=Freq))+
  geom_bar(position="dodge",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Genero", title="PAPA discriminado por género")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)#Lineas punteadas en cada unidad
```


## Análisis descriptivo:

En el gráfico se puede observar las diferentes notas del PAPA discriminado por Género. Se observa que la mayor cantidad de personas del género masculino se concentra en las Notas del PAPA en el intervalo de 3.5 a 3.8. El género femenino por su parte se observa bastante disperso, incluso en algunas notas del PAPA no hay personas del género femenino registradas, como es el caso de la nota de 3.1, 3.2, 4.5 entre otras. 


# PAPA-Edad {.tabset}

## Gráfica
```{r}
#Se seleccionan las fechas de nacimiento y los papas sin NA 
papaedad <- sqldf('SELECT fecha_dep,PAPA FROM encuesta_habitos WHERE PAPA IS NOT NULL')
#Se tratan ls fechas de nacimiento para sacar la edad
papaedad$fecha_dep <- as.Date(papaedad$fecha_dep,"%d/%m/%Y")
papaedad$fecha_dep <- round(as.numeric(Sys.Date() - papaedad$fecha_dep )/ 365,0)
#Se seleccionan las edades con sentido
papaedad <- sqldf('SELECT * FROM papaedad WHERE fecha_dep>1')
#Se cuentan las edades por papa ej: con el papa 3.0 hay 2 personas de 20 años, 1 de 17 años, etc
tablaedadpapa <- sqldf('SELECT fecha_dep, PAPA, COUNT(*) as Cuantos FROM papaedad GROUP BY fecha_dep, PAPA')
#Se convierte la edad en un factor para graficar mejor
tablaedadpapa$fecha_dep <- as.factor(tablaedadpapa$fecha_dep)
#Grafica
ggplot(tablaedadpapa, aes(fill=fecha_dep, x=PAPA, y=Cuantos))+
  geom_bar(position="stack",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Edad", title="PAPA discriminado por Edad")+ 
  theme(legend.title = element_blank())
```


## Análisis descriptivo:

En el gráfico se puede observar los intervalos de las diferentes notas del PAPA discriminado por la Edad. Se observa que las personas con edades alrededor de los 19 a 20 años tiene una nota del PAPA de entre el 3.5 y 4, mientras que las personas con mayor edad como 37 y 38 años tienen un PAPA más bajo, de 3.0 a 3.2. Un caso interesante es la persona con una edad de 64 años que tiene una nota del PAPA en 3.9. Como observación, las personas cuya nota del PAPA es 4.7 siendo esta la nota más alta, tienen 18 años. 

# PAPA-Carrera {.tabset}

## Gráfica
```{r}
#Query en sql para poder sacar datos necesarios para graficar
query <- sqldf('SELECT carrera_dep,AVG(PAPA) from encuesta_habitos GROUP BY carrera_dep')
#Lastimosamente no supe como automatizar para que quede organizado para graficar
#Pero usé la información dada para hacer el data frame a mano
#Data frame a mano
para_graficar <- data.frame(3.7,3.6,3.4,3.5,3.7,3.6,4.1,4.0)
#Nombre de las columnas del data frame
colnames(para_graficar) <- c("Estadistica","Ing administrativa","Ing biologica","Ing de control","Ing de sistemas e informatica","Ing fisica"," Ing industrial","Matematicas")
#Se debe poner cuál es el valor minimo y maximo que puede alcanzar la grafica
para_graficar <- rbind(rep(5,8) , rep(3,8) , para_graficar)
#Grafica
radarchart(para_graficar,axistype=1 , 
 
    #Poligono interno
    pcol=rgb(0.2,0.5,0.5,0.9) , pfcol=rgb(0.2,0.5,0.5,0.5) , plwd=4 , 
 
    #Malla
    cglcol="grey", cglty=1, axislabcol="grey", caxislabels=seq(3,5,0.5), cglwd=0.8,
 
    #Etiquetas o labels
    vlcex=0.8, title=paste("Promedio del PAPA por carrera"), cex.main=1 )
```



## Análisis descriptivo:

En el gráfico se muestra la distribución de la nota del PAPA discriminado por Carrera. Se observa que para las carreras correspondientes a Ingeniería Industrial y Matemáticas el promedio de la nota del PAPA de los estudiantes de dichas carreras es ligeramente mayor que para los estudiantes del resto de carreras. Ingeniería Industrial y Matemáticas cuentas con un PAPA alrededor de 4, mientras que carreras como Ingeniería Biológica tienen un PAPA aproximadamente de 3.5.

# PAPA-Estrato {.tabset}

## Gráfica
```{r}
#Tabla del papa segun el estrato
papaxes <- table(round(encuesta_habitos$PAPA,1), encuesta_habitos$estrato)
#Se debe convertir la tabla en un data frame para poder graficar
graf0 <- data.frame(papaxes)
#Grafica
ggplot(graf0, aes(fill=Var2, x=Var1,  y=Freq))+
  geom_bar(position="stack",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Estrato", title="PAPA discriminado por Estrato")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)#Lineas punteadas en cada unidad
```



## Análisis descriptivo:

En la gráfica se puede observar la distribución de la variable PAPA discriminada por Estrato. Se puede notar que la persona con una clasificación de Estrato 0 su nota del PAPA es mucho más baja. Paras las personas de Estrato 1 se pueden ver las cantidades bastantes dispersas. Hay una mayor concentración de personas de Estratos 2 y 3 en los rangos de entre 3.5 y 4. Para las personas que corresponden al Estrato 4 su rango va desde 3.4 a 3.8 y por último, la persona de Estrato 5 tiene un PAPA de 3.8, siento esta nota la que mayor número de personas tiene.

# PAPA-Computador de mesa {.tabset}

## Gráfica
```{r}
#Tabla del papa segun el computador de mesa
papaxpc <- table(round(encuesta_habitos$PAPA,1), encuesta_habitos$computador_mesa)
#Se debe convertir la tabla en un data frame para poder graficar
graf <- data.frame(papaxpc)
#Grafica
ggplot(graf, aes(fill=Var2, x=Var1,  y=Freq))+
  geom_bar(position="stack",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Computador", title="PAPA discriminado por Computador")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)#Lineas punteadas en cada unidad
```



## Análisis descriptivo:

Como se puede ver en la gráfica de PAPA discriminado por computador de mesa, si cogemos un intervalo en los desempeños más bajos de 3 a 3.6, se puede ver que la mayoría no tiene un computador de mesa, pero esto mismo sucede en los desempeños más alto como lo son el intervalo de 4.1 a 4.7 y tienen los más altos promedios y de esto evidenciamos que tener un computador de mesa o no tenerlo, no es un factor que afecte el desempeño del PAPA.

# PAPA-Portatil {.tabset}

## Gráfica
```{r}
#Igual al anterior
papaxpr <- table(round(encuesta_habitos$PAPA,1), encuesta_habitos$portatil)
graf2 <- data.frame(papaxpr)
ggplot(graf2, aes(fill=Var2, x=Var1,  y=Freq))+
  geom_bar(position="stack",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Portatil", title="PAPA discriminado por Portatil")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)
```



## Análisis descriptivo:

Como se puede notar en la gráfica de PAPA discriminado por portátil, se muestra que no es necesario tener un portátil, para tener un buen desempeño, ya que se ve que algunos de los estudiantes con mejores promedios, no poseen uno y aún así, logran obtener el mejor promedio. 

# PAPA-Internet {.tabset}

## Gráfica
```{r}
#Igual al anterior
papaxin <- table(round(encuesta_habitos$PAPA,1), encuesta_habitos$internet)
graf3 <- data.frame(papaxin)
ggplot(graf3, aes(fill=Var2, x=Var1,  y=Freq))+
  geom_bar(position="stack",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Internet", title="PAPA discriminado por Internet")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)
```


## Análisis descriptivo:

Como se puede notar en la gráfica de PAPA discriminado por internet, vemos que aunque se tenga un internet que sea de plan de datos de celular o alquilado, no hace que esto dependa que tan bien nos desempeñemos, por lo que se muestra en el promedio de 4 y 4.3, respectivamente, que es una demostración de lo dicho anteriormente. También vemos que los estudiantes que tiene un internet propio, están ubicados desde el promedio más bajo, hasta el promedio más alto, lo cual quiere decir, que tenerlo propio, no determina el  desempeño de los estudiantes.

# PAPA-Smartphone {.tabset}

## Gráfica
```{r}
#Igual al anterior
papaxsp <- table(round(encuesta_habitos$PAPA,1), encuesta_habitos$smartphone)
graf4 <- data.frame(papaxsp)
ggplot(graf4, aes(fill=Var2, x=Var1,  y=Freq))+
  geom_bar(position="stack",stat="identity")+labs(x="PAPA",y="Número de personas",fill = "Smartphone", title="PAPA discriminado por Smartphone")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)
```



## Análisis descriptivo:

Como se puede observar en la gráfica del PAPA discriminado por Smartphone, se puede ver que desde el desempeño más bajo, como lo son el promedio de 3, los estudiantes poseen un Smartphone, pero el desempeño más alto, que son tiene un promedio en 47, también lo tienen, lo cual indica que el tener un Smartphone, no afecta o mejora el desempeño del PAPA.

# PAPA-Origen del COVID-19 {.tabset}

## Gráfica
```{r}
#Igual al anterior
papaxcovid <- table(round(encuesta_habitos$PAPA,1), encuesta_habitos$origen_covid_19)
graf5 <- data.frame(papaxcovid)
ggplot(graf5, aes(fill=Var2, x=Var1,  y=Freq))+
  geom_bar(position="stack",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Origen COVID-19", title="PAPA discriminado por Origen COVID-19")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)+ 
  scale_fill_brewer(palette = "Set3")+
  theme(plot.margin= unit(c(0,0,0,0),"cm"),legend.text=element_text(size=6.5))
#El theme ajusta el tamaño de la leyenda y las margenes
```



## Análisis descriptivo: 

Como se muestra en la gráfica de PAPA discriminado por Origen del COVID-19, se puede notar que, las respuestas varían mucho, y determinar de si su promedio varía según las respuestas o respuesta que señaló, no tiene relación porque hay estudiantes con un PAPA, muy bajo, y otros estudiantes con un PAPA más alto, que señalaron la o las mismas respuestas.

# PAPA-Estadisticas del gobierno {.tabset}

## Gráfica
```{r}
#Igual que el anterior
papaxstats <- table(round(encuesta_habitos$PAPA,1), encuesta_habitos$estad_gov)
graf6 <- data.frame(papaxstats)
ggplot(graf6, aes(fill=Var2, x=Var1,  y=Freq))+
  geom_bar(position="stack",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Estadisticas del gobierno", title="PAPA discriminado por Estadisticas del gobierno")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)+ 
  scale_fill_brewer(palette = "Set3")#Colores
```



## Análisis descriptivo:

Como se puede observar en la gráfica del PAPA discriminado por Estadísticas del gobierno, según lo que se puede ver en la gráfica, si cogemos el PAPA más bajo, vemos que la mayoría dice que no, y si cogemos el PAPA más alto, pasa lo mismo, pero esto varía mucho, en cada promedio, lo cual nos dice que tener un alto PAPA o no tenerlo, va a ser muy distinto de determinar que tanto se crea en las estadísticas que muestra el gobierno o se muestre escépticos. 

# PAPA-Hábitos {.tabset}

## Grafica
```{r, fig.height=8, fig.width=10}
dfhabitos <-  pivot_longer(encuesta_habitos, habito_1:habito_17,
               names_to = "num_habito",
               values_to = "habito",
               values_drop_na = TRUE)
d<-gsub('^Algunas.*gos.$',' Estudia con amigos y solo', dfhabitos$habito)
d<-gsub('^Escucha\\D*estudia.$','Estudia con música',d)
d<-gsub('^Estudia.*compañeros.$','Estudia solo soluciones',d)
d<-gsub('^Estudia.*anterior.*examen.$','Estudia solo el día anterior',d )
d<-gsub('^Estudia.*misma.*estudio.$','Ve temas distintos en una sesión',d )
d<-gsub('^Hace.*horario.$','Horario estricto',d )
d<-gsub('^Lee.*texto.$','Lee varias veces un capitulo',d )
d<-gsub('^Los.*estudio.$','Ejercicios con amigos',d )
d<-gsub('^Prefiere.*ejercicios.$','Vistazo a muchos ejercicios resueltos',d )
d<-gsub('^Prefiere.*resueltos.$','Pocos ejercicios bien entendidos',d )
d<-gsub('^Resuelve.*anticipadamente.$','Hace ejercicios sin ver las respuestas',d )
d<-gsub('.*lugar.*fijo.*','Estudia en lugar fijo',d )
d<-gsub('^Solo.*resolver.$','Pregunta a amigos por ejercicios que no pudo',d )
d<-gsub('^Toma.*continuo.$','Hace pausas activas',d )
d<-gsub('^Tomas.*clase.$','Transcribe ejercicios de clase',d )
d<-gsub('^Usa.*resueltos.$','Ve ejercicios hechos en solucionarios',d )
dfhabitos$habito<-d
par(mar=c(5.1, 18, 4.1, 2.1))
boxplot(PAPA ~ habito, data = dfhabitos, las = 1,
        xlab = "Promedio Académico Ponderado Acumulado", ylab = "", 
        varwidth = TRUE,
        horizontal = TRUE,
        notch = TRUE,
        cex.text = 0.7,
        col = "wheat2"
        )
abline(v=c(3,3.5,4,4.5), col='grey80', lty='dotted')
media<- mean(encuesta_habitos$PAPA,na.rm = TRUE)
abline(v=media,col='red',lwd=2)
## La media es de 3.7
```



## Analisis descriptivo:

En la gráfica se pueden observar diferentes boxplot de la variable Hábitos relacionada con el desempeño académico PAPA. Se observa claramente como los estudiantes que tienen algunos de estos hábitos de estudio se encuentran muy por debajo de la media, como lo son el ver muchos ejercicios resueltos, memorizar ejercicios y ver ejercicios hechos en solucionarios. Por el contrario, hábitos como un horario estricto y hacer ejercicios sin ver las respuestas muestran un promedio mucho más alto. Un caso interesante es el hábito de estudiar con amigos que muestra un promedio muy por encima de la media general, pero carece de amplitud y, además tiene datos atípicos muy importantes, como el promedio de 3.0.


# PAPA-Aleatorio {.tabset}

## Gráfica
```{r}
#Igual que los anteriores
papaxal <- table(round(encuesta_habitos$PAPA,1), encuesta_habitos$aleatorio)
graf7 <- data.frame(papaxal)
ggplot(graf7, aes(fill=Var2, x=Var1,  y=Freq))+
  geom_bar(position="stack",stat="identity")+
  labs(x="PAPA",y="Número de personas",fill = "Número", title="PAPA discriminado por número aleatorio")+
  geom_hline(yintercept = 0:20, color="gray",linetype="dashed", size=0.2)+ 
  scale_fill_brewer(palette = "Spectral")#Colores
```





## Análisis descriptivo:

En el gráfico se muestra la distribución de la nota del PAPA discriminado por la variable correspondiente a “Número aleatorio”. El número aleatorio que se ve con mayor frecuencia es el número 7, pero respecto al PAPA se ve bastante disperso, caso diferente se observa con el número 10, cuya concentración está en las personas que tiene un PAPA igual a 3.8 y lo mismo sucede con el número 2, donde la concentración está en la nota del PAPA igual a 3.6.


# Conclusiones:

Es bueno realizar trabajos en equipo porque se comparten ideas, se ayudan mutuamente, se toman decisiones sobre que es lo mejor para hacer, se pueden disminuir las debilidades y potenciar las fortalezas, se pueden pensar mejores estrategias, para que todo quede mejor. La actividad la desarrollamos en conjunto, las dudas que se presentaban las discutíamos entre nosotros mismos hasta llegar a una solución precisa. El trabajo en equipo fluyó, puesto que dividimos la actividad en partes que cada uno desarrollaba, lo que hizo que fuera aún más efectivo. El tema fue interesante para la mayoría de nosotros, por esto, lo hicimos con más voluntad. 
Con respecto a los temas que se tocaron en el trabajo como tal podemos decir que:

* La depuración de la base de datos es esencial para poder facilitar el manejo de estos luego
* Cuando se tienen variables que representen fechas es vital tenerl en un formato uniforme todos los datos de la variable para su conversion a tipo date
* Es importante la identificación de que tipo de dato se necesita que sea cada variable para evitar problemas con el trabajo de estas
* Un tipo de depuración eficaz para resumir o modificar textos son las expresiones regulares, lo cual ahorra mucho tiempo a comparación de si se hiciera a mano
*El apoyo de diferentes paquetes o bibliotecas externas puede llegar a ser de gran relevancia para realizar con mejor eficiencia lo que se tiene planeado, ya sea a la hora de graficar o en la adecuacion de los datos
* Tipos de graficas como los boxplot nos dan más información de la que parece, con sus distintos apartados que nos muestran informacion atipica (outliers) o tambien la separacion por cuartiles

# Recomendaciones: 

- Fue bueno realizar esta actividad y aunque logramos desarrollarla con éxito, se aprecia que un taller tan largo y complejo, como lo fue este, debería consideradarse más, ya que en un trabajo en equipo no estamos solo contando con el tiempo que tenemos nosotros,  sino con el de los otros y esto lo hace más difícil por lo que para agilizar con su elaboración es necesario contar con que:
* La base de datos este en perfectas condiciones con sus respectivas correcciones y modificaciones
* Tener presente las distintas librerias  que se necesitan para realizar correctamente el trabajo
* Utilizar distintos tipos de graficas dependiendo de la informacion que se trate de enfatizar y con qué enfoque

